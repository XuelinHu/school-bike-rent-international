# 前端只需要这一个变量：后端 API 的基地址。
#
# ⚠️ 它是**绝对地址**，不是走 Vite 代理的相对路径（vite.config.js 里那段 proxy
#    实际从未生效，见 AGENT.md 的说明）。所以：
#   · 本机开发      → http://localhost:8032/api
#   · 手机/局域网   → http://<这台机器的局域网 IP>:8032/api
#   · 公网入口      → http://47.120.48.245:18032/api
#
# 部署到手机上时**必须**改掉：留 localhost 的话，请求会打到手机自己身上，
# 智能助手面板会一直转圈（而且因为跨域，控制台只有一条语焉不详的 CORS 报错）。
VITE_API_BASE=http://localhost:8032/api
